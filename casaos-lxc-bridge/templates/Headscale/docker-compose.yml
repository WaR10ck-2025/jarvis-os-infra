version: "3.8"
services:
  headscale:
    image: headscale/headscale:0.23
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "9090:9090"
    command: serve
    volumes:
      - config:/etc/headscale
      - data:/var/lib/headscale

volumes:
  config:
  data:
